# Optional independently installed LiteLLM gateway only; offline tests need no key.
LITELLM_MASTER_KEY=
# Set workspace/audit environment variables explicitly to directories you control.
# AI_CLOUD_WORKSPACE=
# AI_CLOUD_AGENT_AUDIT_LOG=
